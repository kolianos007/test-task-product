import { useState } from "react"

interface IUseFetch {
    url: string,
    options: null | {}
}

// const useFetch: IUseFetch = (url, options) => {
//     const [data, setDate] = useState<null | any>(null)
// }

// export default useFetch